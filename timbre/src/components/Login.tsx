import React from 'react';
import Button from '@mui/material/Button';

const Login = () => {
  return (
    <div>
      <Button variant="contained" color="primary">
        Login with Spotify
      </Button>
      {/* Add form for manual login */}
    </div>
  );
};

export default Login;
